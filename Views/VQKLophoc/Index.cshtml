@model IEnumerable<VuQuangKhai0323.Models.VQKLophoc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VQKSinhvien)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VQKSinhvien.VQKMasinhvien)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VQKTenlop">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VQKTenlop">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VQKTenlop">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
